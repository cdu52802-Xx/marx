# === IDE / 编辑器临时文件 ===
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# === AI 工具本地配置（不同步到 GitHub，避免泄露 token / 个性化设置） ===
.claude/
.codex/
.cursor/

# === 密钥与环境变量（绝对不能 commit！）===
.env
.env.local
.env.*.local
*.key
*.pem
secrets/

# === Python（如果项目是 Python） ===
__pycache__/
*.pyc
venv/
.venv/

# === Node / 前端（如果项目是 Web）===
node_modules/
dist/
build/
.next/

# === 通用临时 / 日志 ===
*.log
tmp/
.cache/
