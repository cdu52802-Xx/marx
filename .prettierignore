node_modules/
dist/
playwright-report/
test-results/
coverage/
package-lock.json

# 项目文档不交给 prettier 管（避免按 printWidth 重排破坏作者手写排版）
*.md

# M4: 脚本生成的 JSON 数据用 JSON.stringify(..., null, 2) 输出
# 跟 prettier 默认格式可能不一致（trailing newline / 缩进），交给脚本管理
src/data/claims.json
scripts/data/

# 静态资源 / 存档 / mockup / cache 不交给 prettier 管：
# - public/m3-archive/  M3 已存档 build artifacts（minified JS/CSS，prettier 展开成 ~4000 行噪音）
# - public/styles/      风格调研 mockup HTML（保留作者手写格式）
# - scripts/sparql/cache/  SPARQL 响应 cache（数据，非源码）
public/m3-archive/
public/styles/
scripts/sparql/cache/
